// Generated by CoffeeScript 1.6.3
(function() {
  game.ToastEntity = me.ObjectEntity.extend({
    init: function(x, y, settings) {
      var targetVector;
      settings.image = 'toast';
      settings.spritewidth = '12';
      settings.spriteheight = '12';
      this.parent(x, y, settings);
      this.maxVelocity = 5;
      this.gravity = 0;
      targetVector = new me.Vector2d(settings.target.x - x, settings.target.y - y);
      targetVector.normalize();
      targetVector.scale(new me.Vector2d(this.maxVelocity, this.maxVelocity));
      return this.setVelocity(targetVector.x, targetVector.y);
    },
    update: function() {
      var res;
      this.vel.x += this.accel.x * me.timer.tick;
      this.vel.y += this.accel.y * me.timer.tick;
      this.computeVelocity(this.vel);
      res = this.updateMovement();
      return this.checkForEnvCollision(res);
    },
    checkForEnvCollision: function(res) {
      if (res.x !== 0 || res.y !== 0) {
        return me.game.remove(this);
      }
    }
  });

}).call(this);
